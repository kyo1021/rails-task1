<!DOCTYPE html>
<html>
  <head>
    <title>Task</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
       <h1>Ruby on Rails スケジュール管理アプリ</h1>
     </header>
    <%= yield %>
  </body>
   <footer>
      <h1>現在の日時 : <%= Time.now.strftime("%Y年%m月%d日") %>    
      <% if current_page?(action: "index") %>スケジュール合計 : <%= @posts.count %><% end %></h1>
    </footer>
</html>
